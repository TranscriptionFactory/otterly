{
  "id": "word-count",
  "name": "Word Count",
  "version": "1.0.0",
  "author": "Carbide",
  "description": "Shows word count in the status bar.",
  "api_version": "1.0.0",
  "permissions": ["editor:read", "ui:statusbar", "events:subscribe"],
  "activation_events": ["on_startup"],
  "contributes": {
    "settings": [
      {
        "key": "update_interval_ms",
        "type": "number",
        "label": "Update interval (ms)",
        "description": "How often to refresh the word count.",
        "default": 2000
      }
    ]
  }
}
